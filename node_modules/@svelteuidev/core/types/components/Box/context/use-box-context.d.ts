import type { SvelteUITheme } from '../../../styles';
import type { CSS } from '../../../styles';
declare type Sx = CSS | ((theme: SvelteUITheme) => CSS);
export declare type BoxSx = Sx | Sx[];
export {};
